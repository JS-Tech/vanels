$("#container section#page .slide.current").html('<%= j render "pages/#{params[:page]}" %>');

document.dispatchEvent(pageFetch);

var height = $("section#home").height() - $("header").height();

window.scrollTo(0, height);

document.dispatchEvent(pageLoad);
