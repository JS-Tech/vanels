<div class="wrap agenda">
  <%= render "pages/buttons/backward", text: "Détails", url: "details" %>

  <div class="content">
    <h1>Agenda</h1>

    <p>L'agenda ci-dessous indique les disponibilités de locations. Il est possible de pré-réserver directement
      à l'aide du formulaire présent en bas de page. La pré-réservation est confirmée après paiement.</p>

    <div id="calendar"></div>

    <h2>Formulaire de pré-réservation</h2>

    <div id="reservation">

      <div id="errors"></div>

      <%= form_for Reservation.new, remote: true do |f| %>

        <%= f.label :date %>
        <%= f.date_field :date, readonly: true %>

        <%= f.label :description %>
        <%= f.text_area :description, cols: 40, rows: 5 %>

        <div class="row">

          <div class="column">
            <%= f.label :firstname %>
            <%= f.label :email %>
            <%= f.label :address %>
          </div>

          <div class="column">
            <%= f.text_field :firstname %>
            <%= f.text_field :email %>
            <%= f.text_field :address %>
          </div>

          <div class="column">
            <%= f.label :lastname %>
            <%= f.label :phone %>
            <%= f.label :zip %>
          </div>

          <div class="column">
            <%= f.text_field :lastname %>
            <%= f.text_field :phone %>
            <%= f.number_field :zip %>
          </div>

        </div>

        <%= f.label :city %>
        <%= f.text_field :city %>

        <%= f.submit %>

      <% end %>

    </div>

  </div>

  <%= render "pages/buttons/forward", text: "Contact", url: "contact" %>
</div>
